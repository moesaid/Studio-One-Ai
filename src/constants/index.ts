/**
 * Global constants.
 * Feature-specific constants belong in src/features/[feature]/constants/.
 */

export const APP_NAME = 'Studio One AI';

export const PAGINATION_DEFAULTS = {
  per_page: 10,
  initial_page: 1,
} as const;
